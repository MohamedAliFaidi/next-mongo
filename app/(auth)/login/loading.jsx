// when defining a laoding component, nextjs js will render this component when the page is loading by default
function Loading() {
    
  return (
    <div>loading</div>
  )
}

export default Loading